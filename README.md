# Fizz Buzz Kata

Usually, I end up with a plethora of folders for various kata attempts, so this time I'm trying something different and using branches instead.

Any attempts that I make will be uploaded into branches in the format of `{language}-{attempt}` i.e. `JS-1`.